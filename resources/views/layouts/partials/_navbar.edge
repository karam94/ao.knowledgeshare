<nav class="navbar is-primary">
  <div class="container">
    <div class="navbar-brand">
      <a class="navbar-item" href="{{ route('home') }}">
        <span class="navbar-title-icon">s </span> &nbsp; &nbsp;
        <span class="navbar-title">Knowledgeshare</span>
      </a>
    </div>

    <div class="navbar-menu">
      <div class="navbar-end">
        <div class="navbar-item has-dropdown is-hoverable">
            @if(username)
            <a href="{{ route('profile', { username: username }) }}" class="navbar-link">
              <img src="https://www.gravatar.com/avatar/{{ gravatar }}" alt="{{ user.username }}"> &nbsp; Hello, {{ username }}!</a>
            @endif
              
            <div class="navbar-dropdown">
              <a href="{{ route('profile', { username: username }) }}" class="navbar-item">
                <span class="icon">
                  <i class="fas fa-user"></i>
                </span>
                <span>My Profile</span>
              </a>

              <a href="{{ route('home') }}" class="navbar-item">
                <span class="icon">
                  <i class="far fa-edit"></i>
                </span>
                <span>Latest Posts</span>
              </a>

              <a href="{{ route('subscriptions') }}" class="navbar-item" style="margin-left: 10px;">
                <span class="icon">
                  <i class="fas fa-user-plus"></i>
                </span>
                <span>My Subscriptions</span>
              </a>

              <a href="{{ route('post/create') }}" class="navbar-item" style="margin-left: 10px;">
                <span class="icon">
                  <i class="fas fa-user-edit"></i>
                </span>
                <span>Add Post</span>
              </a>

              <a href="{{ route('home') }}#latestQuestions" class="navbar-item">
                <span class="icon">
                  <i class="far fa-question-circle"></i>
                </span>
                <span>Latest Questions</span>
              </a>

              <a href="{{ route('question/create') }}" class="navbar-item" style="margin-left: 10px;">
                <span class="icon">
                  <i class="fas fa-user-plus"></i>
                </span>
                <span>Ask Question</span>
              </a>

              <a href="mailto:karam.kabbara@ao.com" class="navbar-item">
                <span class="icon">
                  <i class="fas fa-bug"></i>
                </span>
                <span>Report Bug</span>
              </a>
            </div>

        </div>
      </div>
    </div>
  </div>
</nav>
